
# 🚀 STAMS | Aero Intelligence Launch Manual

دليل التشغيل السريع للمنظومة في بيئة الإنتاج.

## 1. الإعداد الأولي (Environment)
تأكد من وجود ملف `.env` في المجلد الرئيسي يحتوي على:
```env
API_KEY=your_google_gemini_api_key
WA_PHONE_ID=your_whatsapp_id
WA_TOKEN=your_whatsapp_token
```

## 2. الانطلاق الموحد (Docker Compose)
نفذ الأمر التالي لبناء وتشغيل كافة الخدمات في خلفية النظام:
```bash
docker-compose up --build -d
```

## 3. التحقق وحقن البيانات (Seeding)
بمجرد تشغيل الحاويات، قم بتهيئة قاعدة البيانات بالبيانات الأولية:
```bash
docker-compose exec backend npx prisma migrate dev --name init
docker-compose exec backend npx prisma db seed
```

---

## 🔍 روابط الوصول للخدمات:
- **واجهة العميل والشركاء:** `http://localhost:3001`
- **نقطة فحص السلامة (Health Check):** `http://localhost:3000/api/v1/health`
- **التوثيق البرمجي (Swagger):** `http://localhost:3000/api/docs`

## ✅ كيفية التأكد من عمل "الدارة الكهربائية" للنظام:
افتح رابط الـ `/health` في متصفحك. إذا كانت النتيجة `{"status": "UP", "ai_service": "CONNECTED"}`, فهذا يعني أن الباك-أند متصل بنجاح بقاعدة البيانات وبمحرك الذكاء الاصطناعي بايثون.
